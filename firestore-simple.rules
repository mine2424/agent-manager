rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // すべてのドキュメントに対して、認証済みユーザーのみアクセス許可
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}